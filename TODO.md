# TODO

## About Page — Underline actually centered under "ABOUT" this time (resolved)
- [x] **Root cause found: two prior fixes on this exact element (`441.578px` from raw Figma, then `309.664px` from a `translateX(-50%)` correction) both centered against *box/node geometry*, never against the glyphs actually drawn on screen** — "About" renders in Perandory Semi-Condensed, a condensed display face, so its real ink is noticeably narrower than the h1's own 324px CSS box (measured 164px of actual ink vs. a 324px box — glyphs don't remotely fill it), and centering math done against the box was centering against empty space the box has on its right side, not the letters.
- [x] **Measured the real rendered ink directly** (same rigor as the gap fix, extended to horizontal edges this time): screenshotted the rendered h1, drew it to a canvas, and scanned every column for the true leftmost/rightmost dark pixel — not estimated from font metrics, not read off the CSS box. Verified the edges are real, sharp transitions (checked the raw per-column luminance data at both boundaries: a clean jump from ~92/background to 26/ink within 1-2px, not a fuzzy multi-pixel anti-aliased smear that would make the measurement unreliable) before trusting the numbers. Real ink: page-x 334.33–498.33 (164px wide, center 416.33px).
- [x] **`.ab3-title-block hr`'s `left` moved from `309.664px` to `284.413px`** (`416.328125 − 263.83/2`), keeping its existing `263.83px` width unchanged since the measured ink (164px) is narrower than that, not wider. Verified visually: a before/after crop shows the underline now sits with clearly symmetric margins under "ABOUT" on both sides, not shifted right of center as it was.
- [x] **Verified against the real build, not just a static file edit**: the site converted to Eleventy since the last pass on this page (`about.html` is now an `.njk` template pulling copy from `content/about.json` — `npm ci && npx eleventy` builds it to `_site/`, which is what's actually served/deployed). Ran the full build and screenshotted the served `_site/about.html` output, confirmed `hr`'s rendered box matches the CSS exactly, and confirmed no horizontal overflow at 1440px or 390px.

## About Page — Underline gap tightened, caption re-centered, marginalia font self-hosted (resolved)
- [x] **Title underline pulled 12px closer to "ABOUT"**: `.ab3-title-block hr`/`p`'s `top` reduced from `222.6875px`/`223.6875px` to `210.6875px`/`211.6875px` (relative spacing to each other unchanged). Pixel-sampled the real ink, not box math, per the task's instruction: "ABOUT"'s ink bottom was at page-y≈203 but the underline's own ink didn't start until ≈223 — a 20px empty gap reading as two separately-floating elements. Compared 8/10/12/14/16px reductions via hi-res crops; 12px is the largest that still leaves daylight below the letters' bottom serifs — 14px and up started visibly touching/overlapping them.
- [x] **Caption nudged further left, `10px` → `-12px`**: Sarah confirmed `10px` (from the previous pass) was progress but still not centered enough. A first attempt at testing the `-13`/`-14`/`-15px` boundary with a narrow, high-DPI crop gave a false "not clipped" reading — caught by cross-checking against the same wide, isolated-page-load crop method used to find the original `-15px` clipping, which confirmed the real boundary is between `-13px` (leading "M" intact) and `-14px` (clipped). `-12px` keeps a small safety margin from that while reading as genuinely centered under the photo.
- [x] **Sue Ellen Francisco self-hosted, not just re-diagnosed**: this sandbox's `fonts.googleapis.com` request has been failing on a cert error through the environment's proxy for several passes now (documented twice already in this file) — each prior "fix" only worked around it for verification screenshots (`ignoreHTTPSErrors` on the test browser), never actually removed the dependency from the site itself, so Sarah kept seeing the plain fallback font on the real, deployed site. Downloaded the actual file directly from Google Fonts' own CDN (`fonts.gstatic.com/s/sueellenfrancisco/v22/...woff2`, the same URL `fonts.googleapis.com`'s stylesheet itself points to — not re-encoded or substituted) to `assets/fonts/SueEllenFrancisco.woff2`, added a matching `@font-face` in `css/tokens.css` right alongside Perandory's, and removed `Sue+Ellen+Francisco` from the Google Fonts `<link>` on every page that had it (`index.html`, `about.html`, `contact.html`, `journal.html`, `journal-post.html` — the last of which never actually used `--font-marginalia` at all despite loading the family — and `listen.html`, keeping `Crimson+Text` there since that's a separate, deliberate exception per `css/listen.css`'s own comment). IM Fell English stays on Google Fonts everywhere, since it isn't self-hosted.
  - **Verified by screenshot, not just computed style**: loaded `about.html` in a browser context with *no* proxy/cert workarounds at all — confirmed `document.fonts` reports `"Sue Ellen Francisco" loaded` with zero network requests for it, then screenshotted the caption and visually confirmed real cursive handwriting, not the fallback. Repeated on Home's "made by Sarah Milad" credit and Journal's "essays, monthly favs, and random thoughts" subtitle — both render as real handwriting too, confirming the fix holds across every page that uses `--font-marginalia`, not just About.
  - Also corrected a stale comment/README claim (both said Sue Ellen Francisco was "currently only on index.html's annotations") — it's actually used across Home, About, Contact, Journal, and Listen; updated `css/tokens.css` and `assets/fonts/README.md` to say so.
- [x] **Verified all three together**: full-page screenshot, zoomed crops of the title-block area and the polaroid/caption area, no horizontal overflow at 1440px or 390px, an 8x-length bio stress test (page height 7927px) confirming earlier footer/background mechanics still hold, and a mobile screenshot confirming both the caption and the footer tagline render in the real handwritten font with no regression.

## About Page — Single-column bio replaces two-column intro (resolved)
- [x] **Two-column `.ab3-intro-row` replaced with a single-column `.ab3-bio`**: `.ab3-intro-copy` and `.ab3-sarahhtonin-column` (and all paragraph content inside both) removed entirely from `about.html`/`css/about.css`, replaced with new copy Sarah finalized for this pass. Positioned at the same left edge and width (592px) the old left column (`.ab3-intro-copy`) used, so it still sits beside the polaroid the same way — a comfortable reading width, not the former full two-column span (1080px). Hero title block, "Hi! I'm Sarah..." greeting, the polaroid + decorative ephemera cluster, and the footer ("Let's Work Together!" / "Selected Credits & Resume") were left untouched.
- [x] **New subheading treatment added**: "My taste in a nutshell" uses a new `.ab3-bio__subhead` style — IM Fell English Italic, 19px (a step up from the 15px body copy) — rather than reusing Perandory (reserved for page-level headings) or Sue Ellen Francisco (reserved for short handwritten marginalia only).
- [x] **This copy supersedes `BUILD-ROADMAP.md`'s locked phrase "my taste is all over the place."** — correction: the phrase was actually locked in `PROJECT-SPEC.md` §9 ("Approved taste heading"), not `BUILD-ROADMAP.md`. Sarah's approved replacement, **"My taste is a little all over the place, in the best way."**, is now in the bio copy and `PROJECT-SPEC.md` §9 has been updated to record the revision (old wording is still noted there as superseded, not deleted).
- [x] **"Selected Credits & Resume" / TMZ Productions / Tony — confirmed still covered separately**: the footer's `.ab3-resume-card` (desktop) and `.av2m-credits` (mobile) already list "TMZ Productions / Senior Music Coordinator / 2023-2026" independently of the bio, so removing the old bio's mention of TMZ/Tony from the two-column copy doesn't drop that fact from the page. Note: neither resume section names "Tony" specifically (a resume entry lists the employer/role/dates, not a mentor's name) — flagging in case Sarah wants Tony credited somewhere on the page now that the bio no longer mentions him by name.
- [x] **Height vs. polaroid — resolved by the footer-reflow fix below**, not by trimming copy: the bio/footer collision this entry originally flagged was a symptom of the footer being pinned to fixed stage coordinates, not the bio's length itself. See "About Page — Footer now flows below the bio."

## About Page — Footer now flows below the bio (resolved)
- [x] **Split `.ab3-stage` into a fixed decorative header + a normal-flow body region**, same technique already proven on Journal Post (`css/journal-post.css`, `.jp-stage`/`.jp-body-region`) — adapted for this page's literal-fixed-px convention rather than Journal Post's percentage-of-width one, since `.ab3-stage` was never meant to be fluid-width. New structure: `.ab3-page` (shared 1440px column, `overflow:hidden`) wraps `.ab3-stage` (unchanged fixed 1304px header: nav, background textures, "ABOUT" title block, and the header half of the ephemera cluster — green paper/logo/polaroid/caption, all confirmed via measurement to render entirely within the header's own bounds) and a new `.ab3-body-region` (normal-flow sibling, pulled up via `margin-top:-1135px` so it starts exactly where `.ab3-content` used to sit inside the old stage). `.ab3-content` (hero + bio) is now a normal-flow child instead of being absolutely positioned against the stage, so real copy length pushes everything after it down instead of colliding.
- [x] **Footer (`.ab3-cta` + `.ab3-resume-card` + their ephemera) grouped into one `.ab3-footer` block** that flows right after `.ab3-content`, instead of each piece being absolutely positioned against the old fixed stage. Every child keeps its original `left` and gets a new `top` recomputed relative to this container (documented in the CSS comment on `.ab3-footer`) — DOM order (pink strip → cta → resume card → resume link → vert line → blue corner paper → flowers → get-in-touch) is unchanged, preserving the original paint order. `height:352px` + `overflow:hidden` on `.ab3-footer` reproduce the exact crop the pink strip and blue corner paper always had against the old stage's bottom edge, rather than suddenly revealing more of either asset than Figma's own frame ever showed.
- [x] **Decorative ephemera cluster split empirically, not assumed**: measured every cluster element's real rendered bounding box before deciding where each piece belongs. Green paper's bounding box is huge (top:-20 to bottom:1261 relative to the old stage) but isolating it and screenshotting shows only a small top-left triangle is ever actually visible on-canvas — safely stays in the header. Logo and polaroid both render well above where the footer now starts — stay in the header too. Blue corner paper (old top:1044.5) and flowers (old top:1112.4) are footer-adjacent decoration — moved into `.ab3-footer` with the rest of that group.
- [x] **Stress-tested with an 8x-length bio** (same idea as Journal Post's 40-paragraph test): footer correctly tracked the much longer bio, landing at y≈7477 on a 7829px-tall page (vs. y≈1233 on a ~1585px page with today's real copy) — confirming the footer's position is driven by real content length, not hardcoded to look right only for today's specific copy. No horizontal overflow (`scrollWidth === clientWidth`) at either length.
- [x] **Background gap found and fixed via the same stress test**: the header's own `.ab3-bg-foreground` paper texture is clipped by `.ab3-stage`'s fixed 1304px height, so any bio long enough to push past that edge hit a hard seam into flat page background — confirmed via a cropped screenshot at the seam before fixing it. Added `.ab3-body-bg`, a `top:1135px (local); bottom:0` stretchy layer (same technique as Journal Post's `.jp-decor__bg-lower`) that continues coverage for the rest of `.ab3-body-region`, however tall it grows. First attempt tiled the same PNG as a repeating `background-image`, which produced a visible seam (the tile's crop/scale didn't match `.ab3-bg-foreground`'s specific `object-fit:cover` box) — canvas-sampled the actual asset instead of guessing further and found it's close enough to a flat, uniform cream (RGB averages within a couple of points across the whole image) that a plain `background-color: #f1e0c6` match is visually indistinguishable from continuing the real texture, with zero seam-alignment risk. Re-verified with the same 8x stress test: no visible seam anywhere on a 7829px-tall page.
- [x] **Real regression caught and fixed during verification**: moving the bio/footer out of `.ab3-stage` meant the mobile media query's `.ab3-stage { display: none; }` no longer hid them — confirmed via a 390px-viewport screenshot showing the desktop footer rendering on top of `.av2m-page`. Fixed by hiding `.ab3-page` (the new shared wrapper) instead of `.ab3-stage` alone. Re-verified: mobile now renders identically to before this task (`.av2m-page` untouched, `.ab3-page` computes to a zero-size `display:none` box at mobile widths).

## About Page — Background consolidated, pink strip removed, new bordered footer card (resolved)
- [x] **New authoritative reference**: `figma.com/design/FP1ZWOSEAtNuI8w2OG2E7x/About-Me`, node-id 119-2 — a different, updated Figma file from whatever the original desktop build (above) was pulled from. Confirms the pink strip and the `.ab3-bg-torn` band are not part of the current design, and gives the footer's real treatment: node 119:60 is `background: rgba(235,216,184,0.4); border: 1px solid #ac2946;`, not a pink texture.
- [x] **`.ab3-bg-pink-strip` and `.ab3-bg-torn` removed entirely** — the divs, their `<img>`s, and both CSS rule pairs deleted from `about.html`/`css/about.css`. (Note: the pink strip's earlier "remove it" request from a prior pass evidently didn't land before this session moved on to the bio rewrite and the footer-flow fix — this pass finishes that removal for real, confirmed by grepping the codebase for both class names afterward.)
- [x] **`.ab3-body-bg` flat-color patch removed**, no longer needed once the background is one continuous layer (see next item) rather than two separately-positioned pieces that had to line up at a seam.
- [x] **Background consolidated onto `.ab3-page` itself**: `background-color: #f1e0c6` (the same canvas-sampled tone from the previous pass) now lives on the single wrapper that already contains both `.ab3-stage` and `.ab3-body-region`, so it's genuinely one continuous fill behind the whole page — `.ab3-bg-foreground`'s real texture image still paints on top of it for the header specifically, but there's no second element that has to start in exactly the right place to avoid a seam. Verified at both the current bio length and an 8x-length stress test (7857px-tall page): one continuous fill, no band, seam, or color shift anywhere.
- [x] **New `.ab3-footer-card` added**: Figma node 119:60's exact `background`/`border` values, wrapping `.ab3-cta` + `.ab3-resume-card` + `.ab3-resume-link` + `.ab3-vert-line` + `.ab3-get-in-touch` as one bordered unit. Figma's own literal coordinates for this node assumed the old fixed-absolute layout and don't apply now that the page flows, so the box (`left:307px; top:41px; width:1074px; height:325.8px` within `.ab3-footer`) was derived by measuring where those five elements actually render (bounding rect 335–1353 × 69–338.8) and adding 28px of padding on every side — verified with a footer-crop screenshot showing the border cleanly wrapping the content with even padding, not by copying Figma's numbers directly. Blue corner paper and flowers (kept, per this task's "small accent element" allowance) sit just outside the card's left edge, confirmed not to overlap it.
- [x] **`.ab3-footer`'s height increased 352px → 380px** to fit the new card (its bottom edge lands at 366.8px, taller than the old 352px this container was previously sized to) — the old 352px was specifically sized to reproduce the removed pink strip's crop against the stage's old bottom edge, which no longer applies. Confirmed this doesn't change blue corner paper's own rendering: its box was already exactly sized to end at 352px (an unclipped, self-contained image box, not something `overflow:hidden` was actually truncating), so taller footer, unclipped blue corner paper — pixel-identical either way.
- [x] **Stress-tested again at 8x bio length** after all of the above: continuous background with no seam anywhere from top of `.ab3-stage` through the true bottom of the footer, and the bordered footer card renders correctly (same size, same padding) wherever the taller bio pushes it. No horizontal overflow. Mobile re-confirmed unaffected (`.ab3-page` still computes to `display:none` at mobile widths — untouched by this pass).

## About Page — Three precisely-measured desktop fixes (resolved)
- [x] **Title underline position fixed**: `.ab3-title-block hr`'s `left` was missing a `translateX(-50%)` shift Figma's own export applies to this node (node 119:53) — the metadata paragraph right below it already had this baked into its own `left` (285px), the underline didn't. Corrected `441.578125 - (263.828125/2) = 309.664px`. Verified: the underline now sits directly under "ABOUT" and above "me...more or less" instead of shifted right past the title's own edge.
- [x] **Blue corner paper extended to the footer's true bottom edge**: was `top:92.53px` + `height:259.473px` = bottom at 352px, 28px short of `.ab3-footer`'s real 380px height (left over from the last pass's height bump for the new bordered card). Compared both options side by side (stretching height +28px vs. shifting top +28px) via cropped screenshots — shifting down (`top:120.53px`, height unchanged) reads identically well and carries zero distortion risk versus stretching, so that's what's applied. Verified: no more flat gap between the paper and the true page edge, at both the current bio length and an 8x stress test.
- [x] **Polaroid caption repositioned onto the white border, not guessed**: `.ab3-caption`'s `top` moved from `530px` to `560px`. This needed the *real* fonts loaded to verify correctly — this sandbox's font stylesheet request was failing silently (`fonts.googleapis.com` request errored with `ERR_CERT_AUTHORITY_INVALID` through the environment's egress proxy unless the test browser's HTTPS layer trusts the proxy's CA), so every earlier screenshot in this file was actually rendering `.ab3-caption` in a generic serif/cursive **fallback**, not the real "Sue Ellen Francisco" — a much larger handwritten face with different metrics. Fixed for this verification session only (`newContext({ ignoreHTTPSErrors: true })`, no code change) and confirmed the real font was loading (200 responses for both `fonts.googleapis.com` and `fonts.gstatic.com`, `getComputedStyle` reporting `"Sue Ellen Francisco", cursive`) before trusting any pixel measurement. With the real font, `530px` visibly crossed into the photo (the polaroid is rotated 10.87deg, so its white bottom border is a diagonal band, not a simple rectangle) — compared five candidate values (530/540/550/560/570px) via hi-res cropped screenshots; 560px is the first fully clear of the photo at every x, with roughly even white space above and below before the newsprint scrap. Reads as written on the polaroid now, not floating below it.
  - **Flagging for awareness**: any future verification on this page that involves the marginalia font (`--font-marginalia`, "Sue Ellen Francisco") or "IM Fell English" needs the same proxy-trust workaround, or it will silently measure against the wrong font's metrics — this cost real time to catch and wasn't obvious from the network log alone (Playwright's `requestfailed` event didn't fire; the request just errored quietly against `document.fonts`).

## About Page — Title block shifted toward the beige, caption tilt matched to polaroid (superseded — see "About Page — Title/beige overlap fully resolved" below)
- [x] **Caption rotation matched to the polaroid's tilt**: `.ab3-caption`'s `transform` changed from `rotate(3deg)` to `rotate(10.87deg)`, matching `.ab3-deco--polaroid`'s own rotation exactly. Compared 3/6/8/10.87deg via hi-res cropped screenshots rather than assuming the exact match would read best — 10.87deg is the first that puts the caption's text baseline visibly parallel to the polaroid's own diagonal white-border seam (reading as one tilted object, "handwritten on the photo," instead of flat text over a visibly tilted surface), and the text stays perfectly readable at that mild an angle.
- [x] **Title block shifted down 24px as a rigid unit** (`.ab3-title-block h1`/`hr`/`p`, tops 72→96 / 146.7→170.7 / 147.7→171.7px, relative spacing to each other unchanged) — **a compromise, not the full fix this task asked for, flagged rather than silently under-delivered**:
  - Pixel-sampled the actual rendered ink (not box edges, which include several px of font ascent padding that doesn't reflect where text visually starts/ends) to find the real numbers: "ABOUT"'s ink starts only ~3px inside its own box top, so clearing `.ab3-bg-foreground`'s 134px beige seam with any real margin needs roughly a 60px+ shift of the whole block.
  - But "me...more or less"'s ink bottom sits only ~33px above "Hi! I'm Sarah..."'s ink top (196px — `.ab3-hero` is separate normal-flow content in `.ab3-body-region`, not part of this shift, so it doesn't move). Shifting the title block by more than ~28-30px starts visibly overlapping that heading — confirmed by rendering and screenshotting shift amounts from 10px up to 55px, not assumed from the box math alone.
  - **These two goals directly conflict and can't both be fully satisfied by shifting the title block alone.** 24px is the largest shift that keeps a clean ~9px ink-to-ink gap above "Hi! I'm Sarah..." — "ABOUT" is measurably more on the beige than before (its bottom half/underline/metadata line all clearly sit on it now), but roughly the top third of the letters still touches the red stripe, short of "sits fully within the beige area" as asked.
  - **For Sarah**: closing the remaining gap needs one of — trimming/tightening the space between the title block and "Hi! I'm Sarah..." (e.g. reducing `.ab3-content`'s `padding-top` or the title block's own internal spacing, which this task's "preserve relative spacing" constraint ruled out touching), or accepting "ABOUT" partially on the red stripe as the intended look. Not decided here.
- [x] **Verified together**: full-page screenshot and zoomed crops of both areas at the final values — title block clear of "Hi! I'm Sarah..." with the trade-off above visible, caption reading as one tilted unit with the polaroid. No horizontal overflow. Mobile re-confirmed unaffected (neither `.ab3-title-block` nor `.ab3-caption` exist in the `.av2m-*` mobile markup).

## About Page — Title/beige overlap fully resolved, caption re-centered (resolved)
- [x] **The conflict the previous entry flagged is now actually resolved**, by moving both sides instead of just the title block: `.ab3-content`'s `padding-top` increased from `20px` to `90px` (pushing `.ab3-hero`, "Hi! I'm Sarah...", down to create real room), paired with a much larger title-block shift — `.ab3-title-block h1`/`hr`/`p` now at `top: 148px` / `222.6875px` / `223.6875px` (76px total from the original 72/146.7/147.7px, not the previous pass's partial 24px). The two numbers were solved together, not independently, since neither works alone (confirmed by testing several `(padding-top, shift)` combinations and pixel-sampling the actual ink at each — see the CSS comments on both rules for the exact math).
- [x] **Verified with real ink measurements, not box edges**: "ABOUT"'s ink-top now lands at 151px — 17px clear of `.ab3-bg-foreground`'s 134px beige seam (previously only reached 125px, still 9px short). "me...more or less"'s ink-bottom lands at ~239.7px against "Hi! I'm Sarah..."'s ink-top at ~267px — a 27px gap (previously a tight 9px). Both numbers pulled from the same canvas-pixel-sampling technique used throughout this file, not estimated from CSS box positions.
- [x] **Confirmed the larger padding-top doesn't crowd anything else**: `.ab3-hero`'s new position is still well above the polaroid's own bottom edge (~735px) — checked via full-page screenshot, no new awkward gaps or overlaps introduced elsewhere on the page.
- [x] **Caption nudged left, verified against the polaroid rather than guessed**: `.ab3-caption`'s `left` moved from `29px` to `10px`. Compared 29/15/0/-15px via zoomed crops — 10px reads better centered under the photo above it (29px sat noticeably right-of-center within the border), while staying safely clear of the polaroid's own left edge (0px was already right at that edge; -15px visibly clipped the leading "M" off the page).
- [x] **Verified all together**: full-page screenshot, zoomed crops of both the title-block area and the polaroid/caption area, no overlap anywhere (title/beige seam, title/greeting, caption/polaroid border). No horizontal overflow at 1440px or 390px. Re-ran the 8x-length bio stress test (page height 7927px) — footer/background/corner-paper mechanics from earlier passes all still hold, unaffected by this pass's changes. Mobile screenshot confirms `.av2m-*` renders unchanged.

## Contact Page Polish (resolved)
- [x] **Stray star fixed**: `.cv2-deco--moon` doesn't actually overflow `.cv2-header`'s own clip boundary (its bottom edge sits at 295.7px of 305px) — the real issue was a trailing star landing within `.cv2-bg-foreground`'s cream-textured zone (which starts at `top: 43.9344%` of the header), reading as "spilled" even though structurally still inside the header. Fixed with a `clip-path: polygon(...)` on `.cv2-deco--moon` that trims it right at that same 43.9344% boundary. Since `clip-path` applies in the element's own local, pre-rotation coordinate space (rotation happens to the already-clipped shape), the polygon isn't a simple horizontal cut — it's the 43.9344% boundary line inverse-rotated by -36.05° around the box's own center, worked out via `sin(36.05°)·x + cos(36.05°)·y = target` in the box's local coordinates (full derivation in the CSS comment above the rule). Verified with before/after screenshots at 1440px: the trailing star that previously sat in the cream zone is gone, a zoomed crop of the clip boundary shows a clean cut with no fragment spilling past it, and a full-header screenshot confirms no isolated star remains anywhere in the red-striped zone.
- [x] **Note/text overlap fixed**: `.contact-col--social`'s `padding-top` increased from `8rem` to `11rem`. The overlap was real and measured, not assumed: `.contact-note` (the rotated "Find me around" label) is `position: absolute; top: -1rem` and its rotated bounding box extended to 511.36px at 1440px width, while `8rem` padding put `.contact-social-intro` at 481px — a 30.36px overlap, matching the reported ~30.4px. `11rem` moves `.contact-social-intro` to 529px, leaving a verified 17.64px gap (~18px, as estimated). `.contact-note`'s own `top`/`width`/`transform` were not touched, per its own tuning comment.

## About Page - Positioning (resolved)
- [x] Polaroid/logo position — rebuilt from the authoritative Figma frame (node 1:3) using literal px values and a top-left-pivot rotation conversion for the Decorative Ephemera Cluster
- [x] Green paper texture behind polaroid — present
- [x] Blue torn-corner paper near footer — present (nudged left from Figma's literal wrapper position to clear the "Let's Work Together" text column, which the local asset export was clipping — see comment in css/about.css above `.ab3-deco--blue-corner`)
- [x] Logo rendering as two disconnected pieces — fixed by matching Figma's own `object-fit: cover` export technique instead of a custom crop-percentage trick

## About Page - Remaining Polish
- [x] **Pink strip position fixed**: `.ab3-bg-pink-strip`'s `top` changed from `478px` to `952px`, Figma's real value for this node ("pink strip 1") — `left` (-748px), `width` (2429px), and `height` (474px) already matched Figma exactly and were untouched. The strip was sitting across the main body-copy columns; it now sits down in the footer area, behind/around "Let's Work Together!" and "Selected Credits & Resume", as Figma intends. Verified with before/after full-page screenshots at 1440px and a zoomed footer crop.
- [x] **"ABOUT" heading — mostly matched Figma already, one real gap found and fixed**: position, font size, line-height, letter-spacing, and rendered case (Perandory Semi-Condensed only has capital-style glyphs, so "About" in the HTML already displays as "ABOUT") were already correct. Font-*weight* was not: `.ab3-title-block h1` set no `font-weight` at all, so it inherited the browser's bold default for `h1` — since only a Regular/400 cut of Perandory Semi-Condensed is hosted, this rendered as synthetic/fake bold, confirmed visibly heavier in a side-by-side screenshot against `font-weight: normal`. Fixed by adding `font-weight: normal;` explicitly, matching the same "no fake bold" convention already used everywhere else this font appears (Journal, Journal Post, Listen nav — see the resolved "Nav Typography" entry above). Confirmed with the user before applying, since this contradicted an explicit "already matches, no fix needed" instruction.
  - **Related, NOT fixed (out of scope for this pass)**: `.ab3-hero` (the "Hi! I'm Sarah..." h2) and `.ab3-resume-card h2` ("Selected Credits & Resume") have the exact same gap — no `font-weight` set, so both also render with the browser's default bold-for-h2 as synthetic bold. Only the h1 was in scope for this task; these two are flagged here rather than silently fixed.
- [x] **"about me paper 1" texture removed (not restored)** — corrects this item's own premise: this isn't a texture that's "missing" and needs adding back. `.ab3-bg-paper1` already existed in the code (rendering a large tan texture at `left: 195px`, covering most of the main content area), but Figma's real position for this node is `left: 1509px` — entirely past the 1440px page width, meaning Figma's own design never shows it at all. Sarah confirmed removing it entirely rather than repositioning it somewhere invisible. Removed the `.ab3-bg-paper1` div + `<img>` from `about.html` and the CSS rule (`.ab3-bg-paper1` / `.ab3-bg-paper1 img`) from `css/about.css`. Verified with before/after screenshots: the page reads cleanly without it (the lighter foreground-paper texture underneath shows through instead), nothing else shifted or broke.
- [x] **"Selected Credits & Resume" underline — already correct, verified**: `.ab3-resume-card hr` already exists in the markup and is already styled `border-top: 4px solid var(--color-crimson);`, matching Figma's "Line 3" exactly. No change needed.
- [x] **Duplicate/unreferenced asset audit completed, then acted on** (filesystem cross-reference; cleanup performed in a follow-up pass, re-verified against `about.html`/`css/about.css` immediately before each deletion):
  - **Deleted (17 files, confirmed unreferenced, superseded by an already-adopted shared-asset convention or orphaned by this session's own paper1 removal)**: `About Me-Desktop-Final Assets/Nav_Item___{About,Contact,Discover,Home,Journal}___Tape.png` (5 files — desktop nav uses the shared `/assets/images/shared/nav-paper-tab.png` instead, confirmed in `.ab3-nav__tab`); `About Me-Mobile-Final Assets/Tape_bg{,_2,_3,_4,_5}.png` (5 files — mobile nav uses the same shared asset, confirmed in `.av2m-tab`); `About Me-Desktop-Final Assets/Brand___Sarahtonin_Sounds_Logo.png` (desktop logo markup uses `/assets/images/home-v2/home-logo.png` instead — the **Mobile** folder's same-named file is a separate, still-in-use file and was left untouched); `About Me-Desktop-Final Assets/Arrow_1.svg` (the "Get in touch" link uses the HTML entity `&rarr;`, not an image); `About Me-Mobile-Final Assets/Texture___Red_Stripe_bg.png` (mobile page background uses `Texture___Foreground_Paper.png` instead, confirmed in `.av2m-page`); `About Me-Mobile-Final Assets/Polaroid_Container.png` (mobile polaroid markup only references the photo + sun stamp, no separate container image); `About Me-Desktop-Final Assets/imdb-icon_1.png` and `sun_stamp_1.png` (the desktop page has no IMDb-credit or sun-stamp element at all — only the mobile page does, using the non-`_1`-suffixed files); `About Me-Desktop-Final Assets/about_me_paper__1.png` (orphaned by this pass's own `.ab3-bg-paper1` removal, not a pre-existing duplicate leftover). Removed via `git rm` to preserve history.
  - **Archived, not deleted** (unclear origin or unconfirmed duplicate status — moved to `assets/images/about/_archive/` via `git mv` rather than destroyed, per CLAUDE.md's preserve-by-default policy): the 11 loose root-level files — `black-sparkles.png`, `cluster-of-stars-red.png`, `flower.png`, `headphones.png`, `hollow-gold-sparkles.png`, `hollow-gold-stars-2.png`, `leo-constellation.png`, `sarah-blossom.png`, `sarah-green-polaroid.png`, `selfie-bw.png`, `sun-wax.png` (possibly an earlier, separate batch of decorative-ephemera exploration assets predating the "Final Assets" folders) — plus `About Me-Desktop-Final Assets/sarah_green_background_polaroid__1.png` (819KB, older; not byte-identical to the currently-used `sarah green background polaroid  1.png`, 447KB, so archived rather than deleted despite the strong likely-superseded signal).
  - **Verified safe**: fresh repo-wide re-audit (not just re-trusting this entry) immediately before acting — literal filename grep across the whole repo for every one of the 29 files, checking every non-zero hit individually (the only ones found were this file's own documentation text, or the separate still-in-use Mobile-folder logo) — then a local server load of `about.html` at desktop (1440px) and mobile (390px) viewports with Playwright, confirming zero HTTP ≥400 responses, zero JS page errors, and visually-intact screenshots at both sizes after the 17 deletions + 12 moves.

## Nav Typography — Cross-page inconsistency (resolved)
- [x] **Fix applied**: Listen's nav (`css/listen.css` `.listen-nav__tab`) changed from the literal Figma export (`'Crimson Text', Georgia, serif`) to `var(--font-display)` (Perandory Semi-Condensed), matching Journal Landing's `.jv2-nav__tab` and Journal Post's `.jp-nav__tab` exactly — all three nav implementations now agree on one typeface, staying inside the approved 4-typeface system (CLAUDE.md design hierarchy) rather than needing Crimson Text as a one-off exception.
  - `font-weight: 600` (carried over from the old Crimson Text rule) was also removed: only a Regular/400 cut of Perandory Semi-Condensed is hosted (`css/tokens.css`), so 600 triggered a synthetic/fake bold — confirmed visibly heavier than Journal's and Journal Post's identical nav-tape component in a side-by-side screenshot crop of the "LISTEN" tab. Neither of those two sets `font-weight` either, so leaving it unset here (rather than keeping 600) is what actually matches them, per the task's own "adjust only if it visibly looks wrong" instruction.
  - `color: var(--color-near-black)` was already correct and shared across all three; untouched.

## Journal Post Desktop — Full rebuild from new Figma reference (resolved)
- [x] **Full replacement, not a patch**: desktop rebuilt from scratch against `figma.com/design/NmseLluZJR0NqBKcwIq02A`, node 156:553 ("Desktop-Journal Entry"). The entire previous collage-based build is retired — fixed-height `.jp-stage` (`aspect-ratio: 1492/1024; overflow:hidden; isolation:isolate`), the yellow-paper-as-continuous-background tile, and corner-paper-1/2 anchored to the true end of content are all gone, along with every CSS rule and HTML comment that referred to them. This supersedes the five resolved entries below it in this file (Corner Paper 1 position, Page height capped at 1024px, Fill viewport edge-to-edge, bg-lower's torn edges exposed, Below-the-fold corner anchoring, Yellow paper continuous background) — each is tagged **SUPERSEDED** below rather than deleted, so the historical record of what was tried and why stays intact.
- [x] **New architecture**: plain normal document flow, nav down through the last paragraph however long a given essay's body is — no fixed-aspect stage, no `overflow:hidden` clipping content, no absolute-position-everything. Only the small decorative header cluster (`.jp-header`: logo badge + nav, sized via a `padding-top` percentage to its own content height) uses internal absolute positioning, isolated from the rest of the page via `isolation:isolate` — the same technique the old `.jp-stage` needed, applied here from the start instead of being discovered the hard way again.
- [x] **New content-block architecture** (`journal-post.html`, `.jp-article`): Hero (optional) → body ¶s → Polaroid-1 (optional) → body ¶s → pull-quote (optional) → body ¶s → Polaroid-2 (optional) → body ¶s, all ordinary block-level flow siblings. This is a deliberate single-column simplification of Figma's own two-column layout (Figma splits body copy into 5 separate left/right-column text nodes around the polaroids; here the two "right column" text groups are folded into the single-column reading order as the block-4 and block-8 paragraph groups) — chosen because the task's own written spec describes one linear sequence, not two columns, and a single centered reading column is what actually makes "flows up with no gap when a block is absent" simple to guarantee. Verified with Playwright for every combination the task named — body-only, hero-only, hero+polaroid-1, all four present, and a 40-paragraph-per-group stress test (160 paragraphs total, page grew to 7906px) — measuring the actual gap between every pair of adjacent `.jp-article` children in each case: uniformly 32px everywhere, never a doubled or missing gap.
- [x] **Background**: flat `background-color: #fbe6b2` on `.page-journal-post` (Figma's own value for this frame), with `background-paper-base`/`background-paper-lower` layered on top only inside `.jp-header`, sized in `vw` (30vw/460px max each) so they stay near the top regardless of page length — confirmed via the 40-paragraph stress test that the texture band's own bounding box doesn't move or stretch as the page grows. `#fbe6b2` does not use `var(--color-cream)` (`#ded7c1`) — checked directly against the reused texture asset's own warm tan tone, not assumed; the token reads distinctly greyer. Kept as a literal value scoped to this page, same precedent as `css/journal.css`'s own page-specific literal colors.
- [x] **Three deliberate deviations from Figma's literal frame contents**, each made against this task's own written content-block spec rather than the frame's full contents (flagged prominently, not silently applied — see the HTML comment on `.jp-page`'s markup for the full reasoning on each):
  1. Figma's "Texture / Red Stripe" (161:601) is not built — the task's own Background section names exactly two texture layers plus a flat color, and on inspection Figma's own paint order has the opaque flat rectangle sitting on top of both the red stripe and background-paper-base in their one shared visible band, meaning red-stripe would fully hide paper-base there if built — omitting it is what makes the task's own "paper-base adds grain near the top" description actually true.
  2. corner-paper-1 (161:597) is removed entirely, per this task's explicit instruction. Its old unresolved-placement annotation ("Sarah wants this bottom-left, near the polaroid, and larger — needs a fresh look at whether the pivot-corner assumption is wrong") is **resolved by removal** — see the superseded tag on "Journal Post — Corner Paper 1 position" below; there is no larger-size follow-up to track anymore.
  3. Only two of Figma's three star instances (156:563, 156:564, 161:599) are built, matching the task's explicit "two small scribble-star accents" — 161:599 (the near-duplicate closest to 156:563) is the one dropped. Hero's four corner-tape pieces and the pull-quote's tape are similarly not built: the task's own content-block list gives Polaroid-1/2 tape explicitly but describes hero/pull-quote without mentioning tape at all.
- [x] **Photo placeholders, not real photos**: hero/polaroid-1/polaroid-2 use a plain toned placeholder (`#d8c9a3` fill) instead of a real image. Figma's own new pixel exports for these three nodes could not be fetched — this sandbox's egress policy blocks `www.figma.com` asset URLs (confirmed by direct `curl` test: `403` from the proxy, "destination host not allowed by organization's egress policy" — the same constraint already on record for this page's title-underline asset) — and the two existing repo images close enough in size/shape to consider (`article-cheers-to-this.png`, `article-photo-sky-clouds.png`) are already real CMS `featured_image` content for other, specific, unrelated Journal entries (`content/journal/essays.json`, `content/journal/monthly-favorites.json`), so reusing either here would misrepresent someone else's entry as this template's own demo photo. The frame/tape/shadow/caption chrome around each placeholder is real, finished CSS — only the photo itself is a stand-in, swappable the moment Sarah supplies real per-essay images. This is also what resolves the "Hero photo needs to become swappable" item below: frame/tape/shadow are now genuinely CSS, photo is the only variable, for all three image slots (hero counts too, even though the task's original swappability note only named the old single Polaroid Hero).
- [x] **Verified**: full desktop pass at 1440px (all 4 optional blocks present, matching the Figma reference almost exactly) plus the four permutations and stress test named above — zero JS errors, zero failed requests, zero horizontal overflow (`scrollWidth === clientWidth`) in every case. Mobile (`.jpm-*`) confirmed untouched: no `.jpm-*` rule or the mobile markup itself appears in the diff at all (only the desktop-hiding selector inside the mobile media query was renamed from `.jp-stage` to `.jp-page`, matching the new wrapper's name), and a 390px-viewport screenshot after the rebuild shows the mobile Polaroid Hero/nav/essay body/star-divider all rendering exactly as before.
- [x] **Follow-up correction — background reversed, real red-stripe asset wired in**: the "Three deliberate deviations" bullet's #1 (omitting red-stripe, keeping paper-base/paper-lower) turned out backwards. A later attempt to fetch Figma's real paper-base/paper-lower/red-stripe pixels via the MCP `get_screenshot(contentsOnly, enableBase64Response)` path — a genuinely different path than the blocked `curl` (it succeeded; the Figma MCP server fetches over its own network, not this sandbox's) — visually confirmed all three assets, but exposed a real tooling gap: the tool's only two output forms are a `www.figma.com` CDN URL (still blocked for anything fetched from *this* sandbox) or an inline image shown for viewing only, with no available tool to persist those bytes to a repo file. Reported that gap rather than substituting again, per this task's own fallback instruction. Sarah then exported and attached `texture-red-stripe.png` directly (1856×2250) — pixel-diffing it (aligned by aspect ratio, alpha-channel compared) against the file already sitting at `assets/images/journal/texture-red-stripe.png` (already used by Journal Landing's `.jv2-bg-stripe`) showed a mean alpha diff of 0.075/255 — the same asset, the attachment just went through a lossy webp resize in transport. No file write needed; the existing full-resolution file was already correct. Sarah also confirmed directly against the real Figma file that background-paper-base/-lower are hidden/unused there — removed entirely, along with the two substitute textures standing in for them (`sarahtonin_sounds-background_paper_2_1.png` from Listen, `journal/paper.png`).
  - **New markup**: `.jp-header__decor` (aria-hidden, wraps the stripe `<img>` + logo badge) is now a full-cover (`inset:0`) child of `.jp-header`, with `.jp-nav` as its own sibling inside `.jp-header` rather than outside it.
  - **Real bug found and fixed while verifying "nav sits cleanly on the stripe"**: exact `getBoundingClientRect` measurements (not just a screenshot glance) showed `.jp-nav` had been a DOM sibling of `.jp-header` all along, not a child — since the original full rebuild — so it was never actually positioned relative to it; with no positioned ancestor, its `top: 10.7734%` fell back to resolving against the viewport's height (measured 129px into a 1200px test viewport, i.e. exactly 10.7734% of 1200, not the intended 28px into a 259.766px-tall header). This had been silently wrong the whole time; a typical screenshot check doesn't catch a ~100px miss when nav still visually lands "somewhere near the top of the page" either way. Fixed by moving `<nav>` to be a real child of `.jp-header` (next to the new `.jp-header__decor`, not inside it, so nav stays out of the `aria-hidden` decorative div). A second bug surfaced once nesting was fixed: `.jp-header`'s `height` was still `auto` (padding-top alone isn't a "specified height" for descendants' percentage resolution, per spec) — fixed by adding the standard `height: 0` companion to `padding-top` (the same technique used for pre-`aspect-ratio` responsive video embeds).
  - **Real bug found and fixed in the stripe crop itself**: this asset is a torn scrap with irregular edges on all four sides (confirmed via an alpha-channel row scan: opaque fraction ramps 0%→90% over the first ~350px and back down over the last ~250px of its 2250px height), not a clean rectangle with one flat torn edge. An initial `object-position: center bottom` landed inside that ramp, showing bare white torn-paper backing behind part of the nav row instead of solid stripe. Fixed with `object-position: center 30%`, sampling well inside the confirmed-solid middle band — the same "oversize past a clipped boundary so only the solid interior shows" approach Journal Landing's `.jv2-bg-stripe` already uses for this identical asset.
  - **Verified**: header screenshot at 1440px shows all 5 nav tabs sitting cleanly on solid stripe with no torn-paper showing through and no logo/stripe misalignment; the stripe band ends and the flat `#fbe6b2` background picks up immediately below with no gap or visible seam. Full-page (all 4 blocks), the three permutations, and the 40-paragraph stress test (still 7906px, stripe band still pinned at 0–144px regardless of page length) all re-verified after this change — no regressions. Mobile re-confirmed untouched.
- [x] **Follow-up correction — `.jp-page`'s 1492px width cap removed**: `.jp-page { max-width: 1492px; margin: 0 auto; }` was cutting the red stripe (and everything else) off in a centered column on screens wider than 1492px, leaving visible gutters on both sides — the exact same class of bug already fixed once on the old Journal Post build and on the About page, just not yet applied to this rebuild. Changed to `.jp-page { width: 100%; }`; no other layout math needed since every child (nav, logo badge, article column, body copy, polaroids, pull-quote) is already positioned in percentages or its own explicit `max-width` (e.g. `.jp-article`'s deliberate 1092px cap, kept — only the outer page-width cap was the bug). Verified by screenshot (not just bounding-box math) at 1440px (unchanged), 1512px, 1920px, and 2560px: the stripe and logo badge span genuinely edge-to-edge with zero gutter at every width, all 5 nav tabs sit fully on solid stripe at every width (confirmed both by `getBoundingClientRect` containment checks and by looking at the actual screenshots), and `document.documentElement.scrollWidth === clientWidth` holds at all four. Re-ran the 40-paragraph stress test and the three block-permutation checks at 1920px specifically (this change affects the whole page's width, not just the header) — page grew to 8425px, stripe band stayed pinned edge-to-edge and fixed at the top (0–150px) regardless of content length, and all three permutations still showed a uniform 32px gap between every pair of adjacent `.jp-article` children, with zero errors/failed requests/overflow throughout.
- [x] **Follow-up correction — the stripe's own crop had a real torn-corner defect, missed by that pass's screenshot check**: `object-position: center 30%` (chosen in the previous pass) put a genuine torn/diagonal intrusion right at the top-right of the visible crop — invisible at a casual glance in a full-page screenshot, since it fell mostly behind or beside the CONTACT tab, but confirmed real by extracting the exact displayed source-pixel window and viewing it directly, and confirmed to grow sharply worse as viewport width increased (the same fixed-height defect occupying a larger fraction of a shorter crop — 144px tall at 1440 vs 109px at 2560). A full per-row scan of the complete 1856×2250 source found this asset has NO genuinely edge-to-edge-solid horizontal band at all — it's a torn scrap with irregular edges on all four sides. Worse, a first scan pass checked only the alpha channel and still missed part of the problem: this asset's torn edge is frequently rendered as *opaque* off-white/grey paper fiber, not transparency, so alpha alone doesn't catch it — it had to be re-scanned using a color test instead (torn fiber is neutral/cool, red≈blue; every genuine stripe pixel, cream or red, is warm, red clearly greater than blue).
  - **Fix**: switched `.jp-header__stripe` from an `<img>` with `object-fit: cover` (which always maps the asset's full width to the element's box with no way to crop past the bad edges) to a `background-image` div, where `background-size` can zoom independently of the box. The color-based scan's best window (rows 828–1078, a 250px-tall band) needs a 114px margin trimmed off both left and right before the remainder is reliably solid at every row — confirmed at three window sizes (200/220/250px, agreeing within a couple of px). `background-size: 114.851% auto` (1856 / (1856 − 2×120) × 100, 120 rounding 114 up for buffer) zooms past that margin; `background-position: center 42%` centers horizontally and picks the vertical offset keeping the visible window inside 828–1078 at every tested width (877–1039px source rows at 1440, narrowing to 905–1000px at 2560).
  - **Verified properly this time**: at each of 1440/1512/1920/2560px, extracted and viewed the actual pixel region of the source image being displayed (composited over the page's own `#fbe6b2` background, plus a 4× zoomed crop of just the leftmost/rightmost 80px at each width — 8 zoomed edge crops total) — not just a rendered screenshot. Zero torn/white intrusion at any width or either edge. Also re-confirmed via real rendered screenshots (both a tight header crop and a wide corner crop past the CONTACT tab at 2560px, where the old defect was worst) that the fix holds in the browser, not just in the extracted-crop math. Re-ran the full-page check at 1440px, the 40-paragraph stress test and all three block-permutation checks at 1920px, and the mobile check — all unchanged from the previous pass (uniform 32px gaps, zero errors/overflow, mobile untouched).

## Journal Post — Corner Paper 1 position (SUPERSEDED — see "Journal Post Desktop — Full rebuild" above; corner-paper-1 is removed entirely in the new design, not repositioned)
- [x] **Fix applied**, in two passes: (1) `.jp-decor__corner1`'s `top` changed from the hand-derived `-6.8359%` to `50%` (Figma's own composed output for node `3:154`, `y=512` of the 1492×1024 frame, taken directly rather than re-derived). `left`, `width`, `height` were already correct and are unchanged. (2) `transform: rotate(180deg) scaleY(-1)` removed entirely: this asset (`corner-paper-pink.png`) is pre-flattened, same as the Polaroid + Tape assets already handled correctly elsewhere on this page — its tilt is already baked into the image's own pixels, so the CSS rotation was double-rotating it and hiding most of the paper's real visible shape within its own bounding box.
  - **Root cause of the original `top` bug**: the `-6.8359%` value came from hand-computing a top-left-pivot rotation from the node's raw pre-rotation metadata box (`x=859, y=512, w=910, h=582`) — `(x−width, y−height)` = `(−51, −70)` → `top: -70/1024`. That hand-derivation, not Figma's own data, was the mistake: it placed the paper in the upper-left, tucked behind the logo/nav badge.
  - **Confirmed** directly against Figma's own composed output for this node (not re-derived by hand) and cross-checked against Figma's full-frame screenshot, where the paper sits at the bottom-left near the polaroid at its current real size — matching what `top: 50%` now renders.
  - Verified in-browser: `.jp-decor__corner1`'s bounding box moved from `top: -70px` to `top: 512px` (unchanged by removing the transform, since a 180° rotation doesn't change a box's axis-aligned bounding box); screenshot after removing the transform shows the paper's full pink shape clearly visible at the bottom-left near the polaroid — previously, with the transform still in place, only a thin sliver was visible (correctly positioned but the asset's own pre-flattened art was being flipped/rotated into mostly-transparent territory within its box).
  - **Not addressed by this fix**: Sarah separately asked for corner paper 1 to be **larger** — this pass only corrected position/orientation per Figma's own composed data and the asset's real (pre-flattened) nature; sizing is a separate design ask, still open if Sarah still wants it bigger than Figma's current node size.

## Journal Post — Page height capped at 1024px / Background Paper Section 1 (2:78) not positioned correctly (SUPERSEDED — the fixed-aspect `.jp-stage` this entry is about no longer exists; see "Journal Post Desktop — Full rebuild" above)
- [x] **Fix applied**, in two separately-verified steps: (1) `.jp-body-copy` moved out of `.jp-stage` into a new `.jp-body-region` sibling inside a shared `.jp-page` wrapper, with `isolation: isolate` on `.jp-stage` (the proposed cleaner technique below, not a hand-picked z-index) — confirmed via full-page Playwright screenshot (corner paper 1, base background paper, polaroid, megaphone, and all 4 body paragraphs present and correctly layered) and a 40-paragraph long-content test (page grew from the old capped ~1024px to 7894px, scaling with content, no invisible-paragraph regression at any point). (2) `.jp-decor__bg-lower` (2:78) moved into `.jp-body-region` as a wrapper `<div>` (never `top`+`bottom` directly on the `<img>`, per bug #1 below) at `top: 0; margin-top: 59.6514%; bottom: 0;` — confirmed via Playwright bounding-box measurement (`top: 1213.98px`, matching Figma's real `y=1214` relative to body copy's `y=324` exactly) and screenshot showing the paper texture appearing at that exact point, painting beneath the body text.
- [x] **Follow-up regression found and fixed**: after the above landed, the 40-paragraph stress test showed paragraphs render fine through ~paragraph 8, then everything after a certain scroll point went invisible — blank paper texture only. Root cause: `.jp-decor__bg-lower`'s wrapper is `position: absolute` (required so `top:0`/`bottom:0` can stretch it, per bug #1 below), while `.jp-body-copy` was plain non-positioned flow content — CSS's default stacking rule puts **any** positioned element above **any** non-positioned content in the same stacking context regardless of DOM order (`z-index: auto` on a positioned element still outranks ordinary flow). `isolation: isolate` on `.jp-stage` didn't help because both elements live in `.jp-body-region`, entirely outside `.jp-stage`. **Fix**: `.jp-body-copy` now carries `position: relative; z-index: 1`, and `.jp-decor__bg-lower` carries an explicit `z-index: 0` (not just an omitted one, so a future edit can't silently reintroduce this). Verified with a per-paragraph `elementFromPoint` hit-test across all 40 paragraphs (none occluded) plus overlapping full-page screenshots top to bottom.
- [x] **Symptom**: `.jp-stage` (css/journal-post.css) uses `aspect-ratio: 1492 / 1024; overflow: hidden;` — a hard-capped height that never grows with content. Body copy is currently `position: absolute` inside it (unchanged from the original build), so even the current 4-paragraph lorem-ipsum placeholder is already silently clipped by ~30px (its own box reaches y≈1054, past the 1024 clip line) — real essay copy of any real length would be clipped far worse. This is also why Figma node `2:78` ("background paper section 1") never renders meaningfully: its real Figma `y` is `1214`, below the 1024 frame's own height, meaning it's Figma's background for whatever continues *below the fold* on the real (uncropped) page — a fixed-height stage can't represent "below the fold" at all.
  - **Current actual code state** (as of commit `8ac62db`, the last commit on `claude/add-claude-md-file-3fgnk0`): NONE of the fix below is applied yet. `.jp-decor__bg-lower` (2:78) still sits at its original, first-pass position (`top: 2.6367%`, i.e. Figma's raw `y=27` — a value `get_design_context` reported for this node that's a separate, much-smaller discrepancy than the "below the fold" one; it's mostly hidden behind higher z-index decor layers and isn't visibly meaningful either way). Body copy is still plain `position: absolute`.
  - **Diagnosis reached so far**: fixing this needs body copy to stop being absolutely positioned and become a normal-flow element instead, so its real rendered length can drive an ancestor's real height (a CSS/spec fact: absolutely-positioned descendants never contribute to an ancestor's auto-height, and `aspect-ratio` gives `.jp-stage` a height derived purely from width, not content). The plan: move body copy into a new `.jp-body-region`, a normal-flow **sibling** of `.jp-stage` (not nested inside it), pulled up via `margin-top: -46.9169%` (`= -(1024-324)/1492`, using the CSS-spec fact that margin-top/-bottom percentages resolve against the containing block's *width*, not height — keeping this correct at any viewport width) so it visually starts exactly at body copy's real Figma y (324). A shared `.jp-page` wrapper (holding the `max-width:1492px; margin:0 auto` that `.jp-stage` used to carry alone) is needed so that margin percentage resolves against the right width.
  - **Two real implementation bugs hit and fixed along the way** (both empirically confirmed via isolated throwaway test pages before being trusted, not just reasoned about):
    1. An absolutely-positioned `<img>` with `top`+`bottom` both set and `height: auto` **ignores `bottom` entirely** and sizes itself from its own intrinsic aspect ratio instead — confirmed side-by-side against a plain wrapper `<div>` carrying the same `top`+`bottom` (which correctly stretches). So whenever 2:78 is repositioned, it must be a plain wrapper div containing the `<img>` (the `<img>` filling it at `width:100%; height:100%; object-fit:cover`), never `top`+`bottom` on the `<img>` directly.
    2. **Twice**, moving body copy to `.jp-body-region` caused paragraphs 1–3 to render fully invisible (only the tail end of the last paragraph, past where `.jp-stage`'s content ends, showed). Root cause: `.jp-stage`'s decorative children carry explicit `z-index` values (1, 3, 4, 5, 6, 7, 8, 15, 18, 20), and neither `.jp-stage` nor `.jp-page` contains/isolates that range — so anything placed as a sibling that visually overlaps `.jp-stage` (like `.jp-body-region`, via the negative margin-top) gets compared directly against all of those numbers in the same ambient stacking context. A hand-picked `z-index: 10` on `.jp-body-region` "fixed" it once by luck (clearing 1–8) but is fragile and not principled — and a second attempt's `background-color` fallback on the now-higher-stacked region additionally painted over the stage's yellow paper/megaphone/stars in the overlap zone (fixed by removing that fallback; `body`'s own `base.css` background is already the same cream, so nothing is lost below the fold).
  - **Proposed cleaner technique (validated only in an isolated scratch test, e.g. `isolation-test/test.html` — NOT yet applied to the real page)**: add `isolation: isolate;` to `.jp-stage`'s existing rule. This contains `.jp-stage`'s entire internal z-index range so it never needs to be compared against anything outside it — a later, plain DOM sibling (`.jp-body-region`, no z-index of its own needed at all) then naturally paints on top via ordinary DOM order. Confirmed in isolation: a test container with children at z-index 1, 8, and 20 plus `isolation: isolate`, followed by a plain later sibling with no z-index, correctly showed the sibling painting above all three, including the z-index:20 one. This removes the need to hand-pick/maintain a z-index number against `.jp-stage`'s internal layers (currently 10 different values across nav/title-block/decor, and growing).
  - **Once the height mechanism is confirmed stable**, `.jp-decor__bg-lower` (2:78) still needs a real follow-up pass on its own positioning: move it into `.jp-body-region` as `top: 0; margin-top: 59.6514%` (`= (1214-324)/1492`, so it starts below the yellow-paper-covered area, right where Figma's own `y=1214` places it relative to body copy's `y=324`) `; bottom: 0;` on a wrapper div (per bug #1 above), so it stretches to whatever `.jp-body-region`'s real, body-copy-driven bottom edge turns out to be.
  - **Next session should**: re-apply the `.jp-page` / `.jp-body-region` structural split with `isolation: isolate` on `.jp-stage` (not a hand-picked z-index), verifying **after every individual CSS rule change** (not batched) via a full-page Playwright screenshot that corner paper 1, the base background paper, the polaroid, the megaphone, and all 4 body paragraphs are still present and correctly layered — only moving to the next change once each is confirmed. Only after that structural piece is fully stable should 2:78 itself be repositioned.

## Journal Post — Fill viewport edge-to-edge, no gaps (SUPERSEDED — `.jp-page` briefly had a `max-width:1492px; margin:0 auto` cap during the desktop rebuild, then had that same cap removed again per this exact requirement; see "Journal Post Desktop — Full rebuild" above)
- [x] **Fix 1 applied**: `.jp-page`'s `max-width: 1492px; margin: 0 auto;` removed (now just `max-width: none;`), so the page fills 100% of the viewport width at any desktop size instead of sitting in a centered 1492px column with side gutters. Verified via screenshot + bounding-box measurement at 1440px, 1920px, and 2560px — `.jp-page`'s own width matches `document.body.clientWidth` exactly at every size, with all percentage-based decor scaling proportionally (already engineered for "any viewport width" by earlier fixes) and no breakage at the 2560px extreme.
- [x] **Fix 2 applied**: `.jp-decor__bg-lower`'s `margin-top` changed from `59.6514%` (Figma's raw `y=1214` for this asset) to `46.9169%` (`= (1024-324)/1492`, starting the texture exactly at `.jp-stage`'s own clipped bottom edge, frame `y=1024`) — closing a band that previously showed flat page background with text sitting directly on it. Verified with a zoomed pixel-level screenshot of the seam (texture begins immediately, no flat band) and confirmed the 40-paragraph stress test still starts the texture at the same relative point (`gap = bgLower.top − stage.bottom = 0` at every tested width).
- [x] **Follow-up bug found and fixed while verifying the combined result**: after Fix 1, `.jp-decor__bg-lower`'s pre-existing `left: 2.1448%; width: 102.9491%` — a deliberate slight horizontal bleed that was always harmless while this element lived inside `.jp-stage` (clipped by that element's own `overflow: hidden`) — started causing **real horizontal page overflow** once it moved to `.jp-body-region` (which has no clipping) and `.jp-page` lost its width cap: `document.documentElement.scrollWidth` exceeded `clientWidth` by ~5.09% of the viewport width (98px at 1920px, 130px at 2560px) whenever `.jp-decor__bg-lower` had any real rendered height (i.e., whenever body copy was long enough to reveal it) — a scrollbar and content spilling past the right edge, the opposite failure mode from the one being fixed. **Fix**: `left`/`width` changed to `0`/`100%`, exactly filling `.jp-body-region`'s own width with no bleed. Re-verified at 1440/1920/2560px × short/long copy: `scrollWidth === clientWidth` in all six cases, with the vertical seam fix (Fix 2) unaffected.
  - **Note on a separate false alarm chased down during this same verification pass**: a large, visually "flat"-looking butter-yellow area appeared below the short placeholder's last paragraph within `.jp-stage`'s own box at 1920px+ (before the text is tall enough to reach `.jp-decor__bg-lower`). Pixel-sampled and confirmed via toggling `.jp-decor__yellow-paper`'s visibility that this is genuinely `.jp-decor__yellow-paper` (deliberately oversized to 138.4785% height, extending well past the stage's own clip line for exactly this reason) painting its own smoother/less-grainy middle region, layered over `.jp-decor__bg-base` beneath — not a plain CSS color. A full-grid pixel scan of the stage's lower band found zero pixels matching the raw `--color-cream` token. No fix needed here; noted in case it looks suspicious in a future screenshot.

## Journal Post — bg-lower's torn edges exposed at full width (SUPERSEDED — `.jp-decor__bg-lower`/the yellow-tile background no longer exist; see "Journal Post Desktop — Full rebuild" above)
- [x] **Symptom**: `.jp-decor__bg-lower` stretched `assets/images/journal/paper.png` — a photographed paper texture with a real torn/deckled edge baked into its alpha, originally cropped for a spot where it was mostly hidden behind other collage layers — via a single `<img>` with `object-fit: cover`. Once this element became a large, full-width, variable-height fill (see the two entries above), `object-fit: cover` could only crop the existing photo, never invent more paper past its real edges, so the torn edge showed on both the left and right sides at typical desktop widths, exposing flat page background past it.
- [x] **Fix applied**: replaced the single stretched `<img>` with a CSS tiled background on `.jp-decor__bg-lower` itself (`background-image` + `background-repeat: repeat`, both directions — a single 600px-wide safe crop can't cover real desktop widths up to 2560px tested, so horizontal tiling is required, not just vertical). The `<img>` element stays in the markup (out of scope for this fix — see below) but is hidden via `.jp-decor__bg-lower img { display: none; }`.
- [x] **New asset produced**: `assets/images/journal/paper-tile.webp` (1200×1200, ~170KB) — a purpose-made seamless tile. Derivation: a full-grid alpha scan of `paper.png` (1147×1489) found rows 150–1300 fully opaque (no torn edge) across columns 49–1113 with zero non-contiguous rows in that range; a 600×600 crop was taken from well inside that safe interior (columns 150–750, rows 300–900). Made infinitely tileable in both directions via the standard 4-way mirror technique (the tile's four quadrants are the crop and its horizontal/vertical/both-flipped copies): every internal seam is a mirror axis by construction, and the tile's own outer edges are identical to each other (both equal the crop's own column/row 0), so repeat-tiling shows no seam in either direction. Exported as WebP (quality 90) instead of PNG for file size (174KB vs. 1.9MB at equivalent quality) — same format already used elsewhere on the site (`assets/images/listen/background-paper.webp`).
- [x] **Verified**: screenshots at 1440px/1920px/2560px × short (4-paragraph placeholder) and long (40-paragraph stress test) copy — no flat/background-colored area visible anywhere in the `bg-lower` region (left edge, right edge, or bottom) at any combination; `document.documentElement.scrollWidth === clientWidth` held at every width (the horizontal-overflow fix from the entry above is unaffected); tight zoomed crops of both the left and right edges at 1920px show continuous, seamless paper grain with no visible tile boundary.
- [x] **Out of scope, left as-is**: the now-unused `<img src="paper.png">` in `journal-post.html` — this task's instructions restricted changes to `.jp-decor__bg-lower` and its asset in `journal-post.css` / the assets folder, so the markup wasn't touched. A future pass converting this page to the planned Eleventy/Decap CMS template should remove that dead `<img>` element from the HTML.
- [x] **Follow-up correction**: the original 600×600 crop above (columns 150–750, rows 300–900) contained a distinctive orange-tan mark that repeated visibly at the tile's mirror seams — confirmed by tracing the mark's 4 symmetric copies in the built tile back through the mirror math to a single source point at `paper.png` (608, 410). A blur-residual scan (Gaussian-blurred copy subtracted from the original, then clustered by magnitude and red-minus-blue color cast) of the full safe interior found several orange-cast marks scattered across it, the two most prominent at (608, 410) and (734, 631) — both inside a separately-suggested replacement crop at (89, 150), so that crop was rejected too rather than applied as given. Re-cropped from (100, 490, 600×600) instead, chosen to sit with a ≥30px margin from both prominent marks; rebuilt with the same 4-way mirror technique and exported at the same WebP quality/size target (173.7KB vs. the original 174.1KB). Verified via: the same blur-residual scan confirming no prominent mark falls inside the new crop bounds; a direct zoom on the rebuilt tile's own center mirror-cross seam and both wrap-around edges (left↔right, top↔bottom) showing continuous grain with no color-cast mark; and a full render of the 40-paragraph long-copy page at 1920px showing no visible mark anywhere the texture repeats, horizontally or vertically.

## Journal Post — Below-the-fold design: corners should anchor to true content end (SUPERSEDED — both corner papers are removed entirely in the new design, not anchored anywhere; see "Journal Post Desktop — Full rebuild" above)
- [x] **Fix applied**: `.jp-decor__corner1` and `.jp-decor__corner2` moved out of `.jp-decor`/`.jp-stage` into `.jp-body-region`, so this matched pair now anchors to the true, content-driven end of the essay instead of a fixed spot on the 1024px stage. `left`/`width` unchanged (both already resolve against WIDTH, so they carry over identically between `.jp-stage` and `.jp-body-region`, which share the same width via `.jp-page`). `height` changed from a `%` (which would have resolved against `.jp-body-region`'s own content-driven height, making the papers grow/shrink with essay length) to `aspect-ratio` (`910/582` for corner1, `466/376` for corner2 — each element's former width/height percentages converted to the pixel box they implied at the 1492×1024 reference frame), keeping their pixel size purely width-driven as before.
- [x] **Vertical anchor**: `top: X%` replaced with `bottom: 0; margin-bottom: -4.6917%;` (a width-relative percentage, since margin percentages resolve against the containing block's WIDTH per spec — same technique already used for `.jp-body-region`'s own `margin-top` and `.jp-decor__bg-lower`'s `margin-top`). The `-4.6917%` value isn't arbitrary: both corner papers' old `top + height` summed to ~106.836% of the 1024px frame — i.e. both already bled exactly 70px past the stage's own bottom edge by design (computed independently for each element and only found to coincide after the fact) — and 70/1492 = 4.6917% replicates that same visual relationship against `.jp-body-region`'s real bottom edge instead. `z-index` (3, 4) and `pointer-events: none` (now explicit, previously inherited from `.jp-decor`'s own rule) preserve the original stacking/non-interactivity exactly.
- [x] **Follow-up bug found and fixed while verifying**: moving `.jp-decor__corner2` exposed a small pre-existing overshoot (`left: 69.1689% + width: 31.233% = 100.4019%`, always harmless while clipped by `.jp-stage`'s own `overflow: hidden`) as real horizontal page overflow, the same category of bug hit twice already on `.jp-decor__bg-lower`. Fixed by adding `overflow: hidden` to `.jp-page` itself — the one container `.jp-stage` and `.jp-body-region` already share — which restores the exact same clipping these corner papers always had (including truncating the last ~12% of each image the same way `.jp-stage` always did) rather than leaving new overflow or suddenly revealing more of the pink/red paper past the tear than the original design ever showed.
- [x] **Verified**: screenshots at 1440px/1920px × short (4-paragraph) and long (40-paragraph) copy. `document.documentElement.scrollWidth === clientWidth` at every combination. The corner pair's offset from `.jp-body-region`'s real bottom edge measured identical (67.55px at 1440, 90.08px at 1920) regardless of content length, confirming they track the true end of content rather than a fixed page position — visually framing the last paragraph as a closing flourish in both the short- and long-copy cases.

## Journal Post — Yellow paper should extend as the continuous background (SUPERSEDED — the yellow-paper-as-background approach is retired; the new design uses a flat page background-color instead, see "Journal Post Desktop — Full rebuild" above)
- [x] **Fix applied**: `.jp-decor__bg-lower`'s `background-image` swapped from `paper-tile.webp` (the plain cream tile) to a new `assets/images/journal/yellow-tile.webp`, provided by Sarah and already seamless — no mirroring or cropping needed, unlike `paper-tile.webp`. All of `.jp-decor__bg-lower`'s existing positioning (`margin-top: 46.9169%`, full width, `z-index: 0`) and `background-repeat: repeat` (both directions — a single tile can't cover real desktop widths up to 2560px tested) were left unchanged; only the image itself changed. The page now reads as one continuous yellow-paper background from the hero down to the corner pair at the true end of content, replacing the previous "real design, then a different plain paper below the fold" look.
- [x] **Verified**: screenshots at 1440px/1920px × short/long copy, sampling multiple non-adjacent regions down the full length of the long-copy page (not just edges) — no flat gaps or visible internal tiling seams anywhere in `.jp-decor__bg-lower`'s own coverage area. A visible boundary line does exist at the structural junction between `.jp-stage`'s own (separate, pre-existing) `.jp-decor__yellow-paper` torn scrap and `.jp-body-region`'s new continuous tile — this is the exact junction Sarah already reviewed and approved (see the original ask: "the color and blend against the existing torn yellow-paper piece are good"), not a defect in the new tile itself, and distinct from an internal repeat seam within the tile.
- [x] **Asset note**: `assets/images/journal/paper-tile.webp` (the now-superseded plain cream tile) was left in place, unreferenced — no cleanup requested. `yellow-tile.webp` was re-encoded from the provided 2000×1334 PNG to WebP (quality 90, ~377KB) for web delivery; a 2×2 tiling sanity check found only an extremely subtle repeat boundary, well within the "already seamless" bar Sarah's approval implies.

## Journal Post — Title Block underline needs the real asset (still open — desktop rebuild carried this item forward unchanged)
- [ ] `.jp-title-block__rule` (css/journal-post.css) is a flat `var(--color-crimson)` bar standing in for Figma's real textured hand-drawn crimson brush-stroke SVG/PNG (title-underline, node 161:593 in the new "Desktop-Journal Entry" frame — same node role as the old 3:160/"Line 4"). Still couldn't be downloaded as of the desktop full-rebuild pass — this sandbox's egress policy blocks `www.figma.com` asset URLs (confirmed again directly: a `curl` to the asset URL returns `403` from the proxy, "destination host not allowed by organization's egress policy"), and both `get_design_context`/`download_assets` only hand back a Figma-hosted URL to fetch. Geometry (position/width) is exact; only the visual texture is missing. Swap: export the real asset from Figma as PNG/SVG, drop it in `assets/images/journal/`, then replace the `background-color` rule with a `background-image` at the same box.

## Journal Post — Hero photo needs to become swappable (RESOLVED by the desktop full rebuild, see "Journal Post Desktop — Full rebuild from new Figma reference" above)
- [x] Desktop's hero, Polaroid-1, and Polaroid-2 are now genuinely swappable elements — real HTML/CSS frame, tape, shadow, and caption around each, with only the photo itself as a placeholder fill pending real per-essay images (not yet real photos — see the "Photo placeholders, not real photos" bullet above for why, and swap them in once Sarah supplies real images). This resolves the "frame/tape/shadow as CSS, photo as the only variable" requirement this item asked for, for all three of desktop's image slots — a stronger outcome than the single Polaroid Hero this item originally scoped, since the new design supports three independently-optional per-post images instead of one fixed decorative photo.
  - **Mobile is unchanged and still open**: `.jpm-polaroid-hero` still uses the old pre-flattened `article-photo-sky-clouds.png`/`tape-orange.png` pair (photo, tilt, and shadow baked into one file) — this task was explicitly desktop-only. Mobile's own swappable rebuild, if wanted, is a separate future task.
  - The previous "desktop temporarily disabled" stopgap entry (removing `.jp-decor__polaroid`/`.jp-decor__tape` markup to dodge the corner1/polaroid overlap bug) no longer applies: both `.jp-decor__polaroid`/`.jp-decor__tape` and `.jp-decor__corner1`/`.jp-decor__corner2` are gone from the codebase entirely, replaced by the new `.jp-hero`/`.jp-polaroid` markup and CSS described above.

## Listen Page — Follow-ups
- [ ] Only Dreamy is live (`"live": true` in `content/listen/moods.json`); the other 17 are hidden from the selector until Sarah flips them. Only Dreamy has a blurb, description, and Sarah's Picks; for other moods those stay hidden while `UNDECIDED`.
- [ ] Selector note slot (`selectorNote` in `content/listen/moods.json`, `#listen-moods-note`) is empty; Sarah decides the wording and style.

## Listen — now spinning bar
- [x] Built: bottom-pinned bar with the mood's label art, name, and color, a Spotify / Apple Music toggle, and the visible embed (Spotify 80px, Apple Music 175px). Mood data moved to `content/listen/moods.json` with slug IDs. The old hidden 1px Spotify player is archived in `legacy/listen-hidden-spotify-player/`.
- [ ] **Real-embed check still owed.** `open.spotify.com` and `embed.music.apple.com` are now allowed here, but Spotify's player itself still can't load: it also needs `embed-cdn.spotifycdn.com` (player code), `mosaic.scdn.co` (cover art), and `p.scdn.co` (audio previews), which are blocked. Page wiring has been tested against stand-in players only. On a Netlify deploy preview, confirm at desktop and ~390px mobile: (1) Spotify at 80px shows the current track title + artist, and it updates when the track changes; (2) the turntable play button starts/pauses it; (3) playback continues while scrolling to Crash Courses; (4) once any mood has a real Apple Music URL, Apple Music at 175px shows the current track title + artist. If Apple's compact player doesn't, raise `APPLE_HEIGHT` in `js/listen.js` (Apple's own default for playlists is 450).
- [x] **Spotify fallback built:** if the player hasn't loaded after 12s (`SPOTIFY_TIMEOUT_MS` in `js/listen.js`), the turntable's "Loading player…" and the empty bar space become "The player didn't load. Listen on Spotify instead." in IM Fell English, linking to the mood's playlist. A late load removes it. **Copy is a draft awaiting Sarah's approval**, as is IM Fell English vs. the rest of Listen's Crimson Text.
- [ ] Apple Music URLs are `PLACEHOLDER` for all 18 moods; the Apple Music toggle stays disabled per mood until a real URL is added.
- [x] Mobile turntable overlap fixed: `.tt-record` collapsed to 0px tall on mobile (all its layers are absolutely positioned), so the vinyl spilled over the tape, blurb, and description and the deck/tonearm never showed. It now has the turntable art's aspect ratio (1991 / 1538).
- [x] Mood circles converted to 320×320 WebP: 23.86MB → 261KB for all 18. Originals moved to `archive/listen-circle-originals/` (not published).
- [x] Live mood labels are centered when they all fit in the row; overflowing rows stay left-aligned with scroll arrows.
- [x] **1950sMusicAD removed (resolved, not revisited)**: Sarah's call was to remove the element entirely rather than keep chasing the literal Figma edge-bleed effect. Removed from `listen.html` (the `.listen-1950s-ad` wrapper + `<img>`) and `css/listen.css` (`.listen-1950s-ad`/`.listen-1950s-ad img`, their `@media (max-width: 760px)` override, and the explanatory comment above them). The `html:has(body.page-listen) { overflow-x: hidden; }` rule (and `.page-listen`'s own `overflow-x: hidden`) were left untouched and are NOT dead code from this: verified via a Playwright check with all clipping temporarily disabled that other elements on the page (`.listen-hero__logo`, `.listen-hero__title`, `.tt-picks`/`.tt-picks__list`, `.crash-course__collage`, and a few others) still bleed past the viewport's left or right edge and rely on this same clip — `document.documentElement.scrollWidth` still exceeded `clientWidth` by 11px at 1440px width even with the 1950s ad already removed. Verified the gap where the ad used to sit (between Turntable and "An Introduction To…") is now a clean, direct transition with nothing left over, at both desktop and mobile widths.
- [ ] Consider switching the background paper texture from a stretched single image to a small seamless tileable texture with `background-repeat`, would scale to any page length without needing a large export each time. Not urgent, current version works fine.
