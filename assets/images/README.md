# Images

## Present

### Wordmark
- `wordmark-gold.svg` — Sarah's exact established gold wordmark, wired into `index.html`. Never redraw, retype, or reconstruct it.

### Page-specific folders
- `about/` — 10 images (portrait/selfie picks, sparkles/stars, constellation, headphones, sun-wax) for the About page.
- `home/` — 8 images (doodles, tape, scrap paper, stars) for the Home page.
- `journal/` — 4 images (paper/tape textures) for the Journal page.
- `listen/` — 15 images (colored circular mood-selector labels, record player, paper texture) for the Listen page.

### Shared/reusable pools
- `shared/doodles/` — 15 general decorative doodles (records, stars, sun/moon, cassette, etc.).
- `shared/portraits/` — 9 candidate portrait/selfie photos of Sarah.
- `shared/scrapbook-images/` — 15 decorative scrapbook elements (flowers, sparkles, hearts, constellations).
- `shared/tape/` — 3 tape-strip textures (beige, green, pink).

None of these are assigned to specific layout slots yet — that's a future visual-pass decision, not made here. Do not assume any particular file's intended use beyond its folder grouping.

Several files exist in both a page folder and a shared pool with identical content (e.g. `about/sarah-blossom.png` and `shared/portraits/sarah-blossom.png`). This looks like Sarah pre-selecting picks from the shared pool for a specific page rather than an error, so both copies were kept — see the asset-integration chat report for the full list.

## Still pending (not part of this upload)
- `home-poster.jpg` — static poster/fallback frame for the Home hero video, referenced (commented out) in `index.html`.
- Favicon / social-share image set — deferred to launch polish (`ASSET-MANIFEST.md` §13).

Do not substitute placeholder or stock imagery for anything still pending.
