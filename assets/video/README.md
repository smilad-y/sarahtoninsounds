# Video

Pending: Sarah's edited "video option 1" vinyl/turntable footage for the
Home hero background (`ASSET-MANIFEST.md` §1).

Expected filename referenced (currently commented out) in `index.html`:
`home-loop.mp4`. Once a web-optimized export is available, drop it here
and uncomment the `<source>` line in `index.html`.

Evaluate brightness/contrast in the actual browser composition before
any further color grading — don't judge the video in isolation
(`PROJECT-SPEC.md` §6).
