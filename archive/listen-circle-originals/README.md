# Listen mood circle originals

The 18 full-size mood-label PNGs (925px or 1053px square, 0.6 to 1.9MB
each, 23.86MB total) that were in `assets/images/listen/`. The site now
uses 320×320 WebP versions of each at the same path with a `.webp`
extension (261KB total), referenced from `content/listen/moods.json`.

Kept here untouched as the source files for any future re-export. This
folder isn't published with the site (see `.eleventyignore`).
